/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  basePath: '/Portfolio',      
  assetPrefix: '/Portfolio',
};

export default nextConfig;
